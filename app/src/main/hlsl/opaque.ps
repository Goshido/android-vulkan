#include "geometry-pass.ps"


OutputData PS ( in InputData inputData )
{
    return FillGBuffer ( inputData,
        g_instanceData[ inputData._instanceIndex ],
        g_diffuseTexture.Sample ( g_diffuseSampler, inputData._uv ).xyz
    );
}
