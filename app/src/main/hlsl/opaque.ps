#include "geometry_pass.ps"


OutputData PS ( in InputData inputData )
{
    return FillGBuffer ( inputData,
        g_colorData[ inputData._instanceIndex ],
        g_diffuseTexture.Sample ( g_sampler, inputData._uv ).xyz
    );
}
