{
    "version": "2.0.0",

    "tasks": [
        {
            "label": "Validate UI",
            "type": "shell",
            "group": "build",

            "command": {
                "quoting": "strong",
                "value": "${workspaceFolder}/tools/bin/html-validator.exe"
            },

            "args": [
                {
                    "quoting": "strong",
                    "value": "${file}"
                }
            ]
        }
    ]
}